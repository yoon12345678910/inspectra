services:
  - type: web
    runtime: node
    name: inspectra-relay
    plan: free
    buildCommand: corepack enable && pnpm install
    startCommand: node --import tsx apps/relay/src/node.ts
    envVars:
      - key: PORT
        value: 10000
