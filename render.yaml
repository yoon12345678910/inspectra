services:
  - type: web
    runtime: node
    name: inspectra-relay
    plan: free
    buildCommand: corepack enable && pnpm install
    startCommand: pnpm relay
    envVars:
      - key: PORT
        value: 10000
